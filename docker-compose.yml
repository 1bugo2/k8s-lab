version: "3.9"

services:
  redis:
    image: redis
    container_name: redis

  api:
    build: .
    container_name: api
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    depends_on:
      - redis